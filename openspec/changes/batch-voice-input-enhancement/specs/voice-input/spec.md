## 新增需求

### 需求:模式切换
语音录入组件必须支持单人模式和批量模式切换，默认为单人模式。

#### 场景:切换模式
- **当** 用户打开语音录入对话框
- **那么** 系统显示模式切换按钮（单人/批量），默认选中单人模式

#### 场景:单人模式
- **当** 用户选择单人模式并点击录音
- **那么** 系统执行现有的单人语音录入流程，录音内容直接填充到当前选中患者

#### 场景:批量模式
- **当** 用户选择批量模式并点击录音
- **那么** 系统执行批量语音录入流程，录音停止后显示匹配预览界面